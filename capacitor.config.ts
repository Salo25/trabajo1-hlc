import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'trabajo1-hlc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
